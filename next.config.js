/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['s3-us-west-2.amazonaws.com','via.placeholder.com','1.bp.blogspot.com']
    }
}

module.exports = nextConfig
